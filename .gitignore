node_modules/
.DS_Store
.vercel/

# Generated: `npm run mint` rebuilds all 180 SVGs + metadata from source.
out/

# Dev-only comparison tool. Holds crops of the 1978 poster scan, which is not
# ours to redistribute, so it stays out of the repo.
compare/
compare.html
.vercel

# Local-only layouts, not part of the published site
6x3.html
3x3.html
capture.html

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/
contracts/node_modules/

# Never commit keys or RPC endpoints
.env
.env.*
contracts/.env
contracts/.env.*
nine.html
compare/
